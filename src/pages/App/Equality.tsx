import AppNavbar from '@/components/navbar/AppNavbar';

export default function Equality() {
    return (
        <div>
            <AppNavbar section="Equality" dashboard={false} />
        </div>
    );
}
